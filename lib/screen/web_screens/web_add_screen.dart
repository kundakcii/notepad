import 'package:flutter/material.dart';

class WebAddScreen extends StatelessWidget {
  const WebAddScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Add Screen'),
    );
  }
}
